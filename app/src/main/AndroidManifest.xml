<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studenttutormatchapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentTutorMatchApp">
        <activity android:name=".Activities.ContractFormActivity"></activity>
        <activity android:name=".Activities.MakeOfferFormActivity" />
        <activity android:name=".Activities.FindBidsActivity" />
        <activity android:name=".Activities.ListOffersActivity" />
        <activity android:name=".Activities.MessageListActivity" />
        <activity android:name=".Activities.ChatActivity" />
        <activity android:name=".Activities.DashboardActivity" />
        <activity android:name=".Activities.BidFormActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.FIT3077.API_KEY"
            android:value="${FIT3077_API_KEY}" />
    </application>

</manifest>